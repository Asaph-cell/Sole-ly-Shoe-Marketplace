# Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# ============================================
# SUPABASE CONFIGURATION
# ============================================
# Get these from: Supabase Dashboard > Settings > API

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================
# NOTES
# ============================================
# - DO NOT commit .env.local to version control
# - Replace 'your-project-ref' with your actual Supabase project reference
# - Use the ANON key (not the service role key) for frontend
# - Service role key is only used in Edge Functions (set via Supabase secrets)

# ============================================
# EDGE FUNCTION SECRETS (Set via Supabase CLI)
# ============================================
# Run these commands to set Edge Function secrets:
#
# supabase secrets set RESEND_API_KEY=re_xxxxx
# supabase secrets set PESAPAL_CONSUMER_KEY=your_key
# supabase secrets set PESAPAL_CONSUMER_SECRET=your_secret
# supabase secrets set PESAPAL_IPN_URL=https://your-project.supabase.co/functions/v1/pesapal-ipn-listener
# supabase secrets set PESAPAL_CALLBACK_URL=https://solely.co.ke/orders
